#include <bits/stdc++.h>

using namespace std;

#define fast_io ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

int main () {

    fast_io;

    signed int a, b, value;

    cin >> a >> b >> value;

    cout << value;

    return 0;

}